#This directory contains some files that needs to be changed in ESMF 2.2.0r0

# Correct compilation rules for .f90 (and .F) files:
cp build-common.mk ${ESMF_DIR}/build/common.mk

# If you use the NAG compiler under Linux:
# Correct rules for the Linux + NAG platform:
cp build_config-Linux.nag.default-build_rules.mk \
   ${ESMF_DIR}/build_config/Linux.nag.default/build_rules.mk

# This last file may require further platform specific changes.
# The location of the NAGWARE library may have to be modified.
# For older versions of the NAG library (e.g. on grendel)
# replace the -lf96 with -lf95 (older NAG 4.0 library).
